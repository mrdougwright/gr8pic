<% if current_user.nil? %>
	<%= render "signup_form" %>
	
	<br />
	
<% else %>
	<%= image_tag @photo %>
<% end %>
<!-- This needs work. Trying to get individual photos to show large view from link from history (photos#index) -->